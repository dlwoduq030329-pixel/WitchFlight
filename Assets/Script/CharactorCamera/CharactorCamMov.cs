using UnityEngine;

public class CharactorCamMov : MonoBehaviour
{
    [SerializeField]
    Vector3[] camPos;
    [SerializeField]
    Vector3[] camRot;

    int camIndex = 0;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
